
  # kualala

  This is a code bundle for kualala. The original project is available at https://www.figma.com/design/uIls2cl2jN87ddIgz1FyqX/kualala.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  